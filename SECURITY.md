# 🚨 비상 매뉴얼 — 키 노출 의심 시

> "키가 노출된 것 같아요" 라고 말하면 Claude Code는 이 문서 순서대로 안내합니다.
> 당황하지 말고 **1번부터 순서대로** 진행하세요.

---

## 1단계 — 어떤 키가 노출됐는지 확인

아래 중 어디에 해당하는지 먼저 확인합니다.

| 종류 | 저장 위치 | 형태 예시 |
|---|---|---|
| OpenRouter API 키 | `.env` | `sk-or-v1-***` |
| Oracle 서버 SSH 키 | `~/.ssh/oracle-server.key` | 개인키 파일 (`-----BEGIN ... PRIVATE KEY-----`) |
| WordPress 계정/비밀번호 | `.env` 또는 별도 메모 | 관리자 ID/PW |
| 기타 (DB, 결제, 이메일 등) | `.env` | 서비스별 상이 |

확인이 안 되면 "모르겠어요, 같이 확인해봐요"라고 말하고 `.env` 파일 내용을 마스킹해서 함께 점검합니다.

---

## 2단계 — 즉시 폐기 (Revoke)

**노출된 키는 재사용하지 말고 반드시 폐기부터 합니다.**

- **OpenRouter**: [openrouter.ai](https://openrouter.ai) → Keys 메뉴 → 노출된 키 **Delete/Revoke**
- **Oracle Cloud (SSH 키)**:
  1. Oracle Cloud 콘솔 → Compute → 인스턴스 → 해당 서버
  2. 필요 시 인스턴스 재부팅 없이 `~/.ssh/authorized_keys`에서 노출된 공개키 제거 (서버 접속 가능할 때)
  3. 접속이 안 되면 콘솔 접속(Console Connection) 기능으로 진입 후 조치
- **WordPress**: 관리자 대시보드 → 사용자 → 비밀번호 즉시 변경, 필요 시 다른 관리자 계정 로그아웃 처리
- **기타 서비스**: 해당 서비스의 API 키/토큰 관리 페이지에서 즉시 삭제

---

## 3단계 — 재발급

- 각 서비스에서 **새 키/비밀번호를 발급**받습니다.
- 새 키는 절대 대화창에 평문으로 붙여넣지 말고, 바로 `.env` 파일에 저장하도록 안내받으세요.

---

## 4단계 — 환경변수 교체

1. `~/claude-workspace/.env` 파일을 엽니다 (Claude Code에게 직접 열라고 하지 말고, 본인이 편집기로 열어 수정하는 것을 권장).
2. 옛 키 값을 새 키 값으로 교체합니다.
3. 이 키를 사용하는 프로그램/스크립트를 재시작합니다.
4. Claude Code에게 "`.env` 키 다시 마스킹해서 확인해줘"라고 요청하면 정상 반영 여부를 마스킹된 형태로 확인해 줍니다.

---

## 5단계 — 사용 이력(악용 여부) 확인

- **OpenRouter**: Dashboard → Usage/Activity 에서 본인이 모르는 요청·급격한 사용량 증가가 있는지 확인
- **Oracle Cloud**: Console → Governance → Audit 또는 인스턴스의 로그인 로그(`/var/log/auth.log`, `last` 명령어)로 낯선 IP 접속 여부 확인
- **WordPress**: 관리자 → 활동 로그(플러그인 있는 경우) 또는 최근 게시물/사용자 변경 이력 확인
- 이상 징후 발견 시: 해당 서비스 고객센터/보안팀에 즉시 신고

---

## 예방 수칙 (참고)

- `.env`, `*.key`, `*.pem`, `id_rsa`, `credentials` 파일은 git에 절대 커밋하지 않습니다. (`.gitignore` 필수)
- 키는 항상 마스킹된 형태(`sk-or-v1-***`)로만 공유/기록합니다.
- 코드에 키를 하드코딩하지 않고 항상 환경변수로 참조합니다.

---

*이 문서는 CLAUDE.md의 "비상시 — 키 노출 의심" 절차를 구체화한 것입니다.*
